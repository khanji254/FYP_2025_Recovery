\section{Methodology}
\label{sec:methodology}

This chapter presents the \textit{what, why, and how} of the N4 recovery-system design process. In line with the interim-report guidelines, this chapter contains design considerations, calculations, circuit and CAD development workflow, and implementation planning for each subsystem.

\subsection{Design Approach and Subsystem Decomposition}

The project follows a design-build-test iteration sequence adapted from the proposal and refined for interim implementation:
\begin{enumerate}
    \item Define subsystem-level requirements from mission constraints.
    \item Generate candidate mechanical, electrical, and software concepts.
    \item Select implementable concepts using performance and manufacturability criteria.
    \item Build CAD and circuit designs for the selected concepts.
    \item Implement embedded firmware architecture and integration interfaces.
    \item Execute staged bench and system-level validation tests.
\end{enumerate}

The methodology is organized into five implementation modules:
\begin{enumerate}
    \item Telemetry link and antenna strategy,
    \item Pyrotechnic deployment and ignition control,
    \item Power and feedback instrumentation,
    \item Mechanical containment and avionics support structure,
    \item Embedded software architecture and communication control.
\end{enumerate}

\subsection{Telemetry Module Design Methodology}

\subsubsection{Telemetry Module Scope}

The telemetry module covers flight-to-ground data transmission hardware, antenna strategy, communication reliability constraints, and integration interfaces to the ESP32 flight computer firmware \cite{xbee_manual, n4_xbee_integration, n4_comm_architecture}.

\subsubsection{Telemetry Design Considerations}

Design considerations for telemetry selection are:
\begin{itemize}
    \item Minimum operational range of at least 5~km line-of-sight.
    \item Robust packet delivery with low drop rate in dynamic flight attitude.
    \item Simple and deterministic integration with ESP32 UART telemetry output.
    \item Field-deployable ground-station setup without complex infrastructure.
    \item Compatibility with future communication redundancy (beacon/MQTT fallback).
\end{itemize}

\subsubsection{Telemetry Parts List}

\begin{table}[htbp]
\centering
\caption{Telemetry Module Parts List}
\label{tab:telemetry_parts}
\small
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{@{}p{3.3cm}|p{3.5cm}|p{6.4cm}@{}}
\toprule
\rowcolor{gray!22}\textbf{Part} & \textbf{Specification} & \textbf{Function in Module} \\ \midrule
XBee-PRO 900HP (S3B) & 900~MHz ISM, up to +24~dBm Tx power & Primary long-range telemetry radio for rocket and base station links \\
ESP32 Flight Computer & Dual-core MCU, UART interfaces & Generates and streams telemetry packet data to the radio interface \\
900~MHz Duck Dipole Antenna & Approx. +2~dBi omnidirectional gain & Baseline antenna on rocket and ground for initial long-range tests \\
XBee Shield/UART Interface & Transparent serial routing & Hardware adaptation between ESP32 UART and XBee module pins \\ \bottomrule
\end{tabular}
\end{table}

\subsubsection{Telemetry Design-Option Comparison}

\begin{table}[htbp]
\centering
\caption{Telemetry Design Comparison Against Module Considerations}
\label{tab:telemetry_option_compare}
\small
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{@{}p{2.8cm}|p{2.0cm}|p{2.3cm}|p{2.5cm}|p{3.7cm}@{}}
\toprule
\rowcolor{gray!22}\textbf{Option} & \textbf{Range Capability} & \textbf{Integration Effort} & \textbf{Reliability Potential} & \textbf{Assessment vs Requirements} \\ \midrule
XBee 2.4~GHz & Low--medium & Low & Medium & Insufficient long-range margin for target mission envelope \\
LoRa (generic modules) & Medium--high & Medium & Medium--high & Good range but added integration and protocol adaptation overhead \\
ESP-NOW/Beacon only & Medium (field dependent) & Medium & Medium & Useful backup path but not preferred as sole primary long-range channel \\
\textbf{XBee-PRO 900HP} & \textbf{High (5~km+ target)} & \textbf{Low--medium} & \textbf{High} & \textbf{Best match to range, robustness, and implementation constraints} \\ \bottomrule
\end{tabular}
\end{table}

\subsubsection{Chosen Telemetry Design and Justification}

The selected telemetry hardware is the \textbf{Digi XBee-PRO 900HP} operating in transparent UART mode because it provides strong range performance, straightforward ESP32 integration, and robust field operation with minimal protocol complexity \cite{xbee_manual, n4_xbee_integration, n4_comm_architecture}.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.34\textwidth]{images/xbee_module.jpg}
\caption{XBee-PRO 900HP: primary long-range telemetry radio}
\label{fig:selected_xbee_module}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.58\textwidth]{images/XBeeDiagram.png}
\caption{XBee telemetry architecture: flight computer to ground station link}
\label{fig:method_xbee_system}
\end{figure}

\subsubsection{Antenna Options and Selection Using Link-Budget Math}

Candidate antennas considered for the telemetry link are summarized in Table~\ref{tab:antenna_compare_method}.

\begin{table}[htbp]
\centering
\caption{Antenna Options for Telemetry Module}
\label{tab:antenna_compare_method}
\small
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{@{}p{3.1cm}|p{1.8cm}|p{2.4cm}|p{4.8cm}@{}}
\toprule
\rowcolor{gray!22}\textbf{Antenna Type} & \textbf{Typical Gain} & \textbf{Pattern} & \textbf{Methodology Assessment} \\ \midrule
Duck Dipole (900~MHz) & $\sim$2~dBi & Omnidirectional & Baseline choice due to simplicity and tolerance to vehicle rotation \\
Patch Antenna (900~MHz) & $\sim$7~dBi & Directional & Higher gain but stricter pointing and build tolerance requirements \\
Bi-Quad (900~MHz) & $\sim$10--11~dBi & Directional & Considered as a base-station upgrade candidate to improve null-zone robustness \\ \bottomrule
\end{tabular}
\end{table}

The selected baseline antenna pair for current implementation is the 900~MHz duck dipole. Its feasibility is checked with the first-order link budget:
\begin{equation}
\mathrm{FSPL(dB)} = 20\log_{10}(d) + 20\log_{10}(f) - 147.55
\label{eq:fspl}
\end{equation}

For $d = 5000$~m and $f = 915\times10^6$~Hz,
\begin{equation}
\mathrm{FSPL} \approx 105.66\,\mathrm{dB}
\label{eq:fspl_value}
\end{equation}

With $P_{tx}=24$~dBm, $G_{tx}=2$~dBi, and $G_{rx}=2$~dBi:
\begin{equation}
P_{rx}=P_{tx}+G_{tx}+G_{rx}-\mathrm{FSPL}
\label{eq:prx}
\end{equation}

\begin{equation}
P_{rx} \approx -77.66\,\mathrm{dBm}
\label{eq:prx_value}
\end{equation}

Given receiver sensitivity $P_{sens}=-110$~dBm, the link margin is:
\begin{equation}
\mathrm{Margin}=P_{rx}-P_{sens}=+32.34\,\mathrm{dB}
\label{eq:margin}
\end{equation}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.46\textwidth]{images/dipole.PNG}
\caption{900~MHz duck dipole: baseline rocket telemetry antenna}
\label{fig:dipole_method}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.50\textwidth]{images/donut_pattern.png}
\caption{Dipole radiation pattern: omnidirectional main lobe with axis null zones}
\label{fig:dipole_wave_profile}
\end{figure}

To mitigate dipole null effects at the ground segment, a \textbf{Bi-Quad antenna is being considered as a potential base-station alternative} for improved directional gain and better coverage across critical tracking sectors \cite{arrl_antenna_book, pozar_microwave}.

\subsection{Electrical Module with Deployment Submodule Methodology}

\subsubsection{Electrical + Deployment Module Scope}

This module covers onboard signal interfacing, telemetry electrical routing, power/continuity monitoring, and pyrotechnic actuation electronics. It is paired with the deployment submodule because ignition-drive design depends directly on electrical interface limits and power budget constraints.

\subsubsection{Design Considerations}

Key electrical/deployment design considerations are:
\begin{itemize}
    \item The flight computer has limited available pins, requiring protocol-level optimization between UART and I$^2$C paths.
    \item Telemetry radio (XBee) requires hardware serial, with remapped RX/TX pins on ESP32 to preserve other interfaces.
    \item Power monitoring and chute-state sensing require additional channels beyond native MCU pin allocation, motivating ADC expansion over I$^2$C.
    \item The electronics stack must fit inside the 95~mm avionics bay and align with holder mounting geometry and rod clearances.
    \item The deployment channel sees high-voltage transients (up to 15~V at chute lines), while BMS-limited bus operation is capped around 14~V.
    \item Pyro switching must be compatible with 3.3~V logic drive and sustain current pulses without excessive heating.
\end{itemize}

\subsubsection{Protocol and Interface Option Comparison}

\begin{table}[htbp]
\centering
\caption{Electrical Interface Options for Pin-Constrained Flight Computer Integration}
\label{tab:electrical_protocol_compare}
\small
\begin{tabular}{@{}p{2.8cm}|p{2.6cm}|p{2.7cm}|p{2.8cm}|p{2.8cm}@{}}
\toprule
\rowcolor{gray!22}\textbf{Interface Option} & \textbf{Pin Usage} & \textbf{Suitability for XBee} & \textbf{Suitability for Monitoring} & \textbf{Assessment} \\ \midrule
UART-only for all peripherals & High & Excellent for XBee stream & Poor channel scalability & Not optimal for mixed telemetry + sensing architecture \\
I$^2$C-only for all peripherals & Low & Poor for high-rate transparent radio stream & Good for ADC expansion & Not suitable as sole interface due to XBee serial requirements \\
\textbf{Hybrid: UART + I$^2$C (chosen)} & \textbf{Balanced} & \textbf{Best for XBee hardware serial} & \textbf{Best for ADS1115 expansion} & \textbf{Best fit for pin limits and subsystem coexistence} \\ \bottomrule
\end{tabular}
\end{table}

\subsubsection{Chosen Electrical Architecture}

The selected architecture uses hardware UART for XBee telemetry and I$^2$C for ADS1115-based monitoring expansion. This preserves deterministic serial throughput for radio transmission while offloading analog channel scaling to an external converter integrated on the existing I$^2$C bus \cite{xbee_manual, esp32_datasheet, ads1115_datasheet, n4_xbee_integration, n4_comm_architecture}.

\subsubsection{Dimensional Constraints and Power Requirements}

\begin{table}[htbp]
\centering
\caption{Electrical Module Constraints and Design Power Budget}
\label{tab:electrical_constraints_power}
\small
\begin{tabular}{@{}p{3.8cm}|p{3.0cm}|p{5.9cm}@{}}
\toprule
\rowcolor{gray!22}\textbf{Constraint/Parameter} & \textbf{Design Value} & \textbf{Implication} \\ \midrule
Avionics bay diameter envelope & 95~mm & PCB and daughter-board placements constrained by cylindrical packaging \\
PCB footprint target & up to 70~mm \(\times\) 70~mm & Allows routing margin while preserving service clearance \\
XBee supply rail & 3.3~V (radio domain) & Requires clean low-voltage rail and UART-level compatibility \\
Main pyro bus & 14--15~V domain & High-energy actuation path isolated from logic rail \\
Chute line deployment potential & up to 15~V (observed), BMS-limited to \(\sim\)14~V nominal cap & Monitoring divider and ADC scaling must tolerate both cases \\ \bottomrule
\end{tabular}
\end{table}

\subsubsection{Electrical Materials List}

\begin{table}[htbp]
\centering
\caption{Electrical and Deployment Submodule Materials}
\label{tab:electrical_materials}
\small
\begin{tabular}{@{}p{3.2cm}|p{2.6cm}|p{6.9cm}@{}}
\toprule
\rowcolor{gray!22}\textbf{Component} & \textbf{Key Rating/Interface} & \textbf{Role in Selected Design} \\ \midrule
ESP32 flight computer & 3.3~V logic, UART + I$^2$C & Core control and communication processing \\
XBee-PRO 900HP & UART transparent mode & Long-range telemetry radio interface \\
ADS1115 ADC & 16-bit, I$^2$C interface & Voltage and chute channel expansion for pin-limited MCU \\
IRLZ44N MOSFET & Logic-level gate, low \(R_{DS(on)}\) & Pyro-channel low-side switching for PWM ignition \\
Voltage divider network & 47~k\(\Omega\) / 10~k\(\Omega\) & Scales 14--15~V monitoring signals to safe ADC range \\
Nichrome igniter element & Resistive heating load & Converts electrical pulse to ignition heat \\
BMS module & Over-voltage/under-voltage protection & Caps and protects battery bus for controlled deployment operation \\ \bottomrule
\end{tabular}
\end{table}

\subsubsection{Circuit Development Workflow}

Electrical implementation follows a staged concept-to-production flow:
\begin{enumerate}
    \item Breadboard and wiring validation,
    \item Fritzing-level integration drafting,
    \item EasyEDA schematic and PCB capture,
    \item Bench verification and revision control.
\end{enumerate}

The Fritzing layouts below document the early wiring verification stage. Final EasyEDA schematics and fabricated PCB designs are presented in Chapter~4.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.62\textwidth]{images/Base_Station_Design.jpeg}
\caption{Base-station interconnect layout (Fritzing)}
\label{fig:fritzing_base}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.62\textwidth]{images/Rocket_Extension_Design.jpeg}
\caption{Rocket XBee extension wiring layout (Fritzing)}
\label{fig:fritzing_rocket}
\end{figure}

\subsubsection{Power-Monitoring Calculations (ADS1115)}

The monitoring path uses a 47~k\(\Omega\)/10~k\(\Omega\) divider into ADS1115 \cite{ads1115_datasheet}. Divider output is:
\begin{equation}
V_{ADC}=V_{in}\frac{10}{47+10}=V_{in}\frac{10}{57}
\label{eq:vadc_divider}
\end{equation}

For BMS-capped nominal maximum $V_{in}=14$~V:
\begin{equation}
V_{ADC,14}=14\frac{10}{57}\approx2.456\,\mathrm{V}
\label{eq:vadc14}
\end{equation}

For observed chute-line deployment case $V_{in}=15$~V:
\begin{equation}
V_{ADC,15}=15\frac{10}{57}\approx2.632\,\mathrm{V}
\label{eq:vadc15}
\end{equation}

With ADS1115 configured at \(\pm4.096\)~V full-scale, digital code estimate is:
\begin{equation}
N\approx\frac{V_{ADC}}{4.096}\times32767
\label{eq:adc_counts}
\end{equation}

\begin{equation}
N_{14}\approx19650,\qquad N_{15}\approx21060
\label{eq:adc_counts_values}
\end{equation}

These ranges provide clear monitoring margin below ADC full-scale while preserving sensitivity to deployment-state transitions.

\subsubsection{Deployment PWM and MOSFET Selection}

The pyro channels are driven through PWM-controlled MOSFET stages to avoid nichrome burnout and improve reusability across repeated ground tests \cite{pwm_heating, barr_pwm, n4_pyro_control, n4_pwm_config}.

\textbf{(a) Nichrome wire element characterisation:}

The igniter elements use 26~AWG nichrome resistance wire with a linear resistance of approximately 8.5~$\Omega$/m. For a representative bridge-wire element of length $l_{e}=0.10$~m (10~cm), the igniter element resistance is:
\begin{equation}
R_{elem}=\rho_{NiCr}\,l_{e}=8.5\times0.10=0.85\,\Omega
\label{eq:relem}
\end{equation}

At a target drive voltage $V_{target}=6.0$~V (controlled via PWM duty cycle), the steady-state current delivered to the element is:
\begin{equation}
I_{elem}=\frac{V_{target}}{R_{elem}}=\frac{6.0}{0.85}\approx7.06\,\mathrm{A}
\label{eq:ielem}
\end{equation}

Corresponding instantaneous power dissipation in the wire at this operating point:
\begin{equation}
P_{wire}=\frac{V_{target}^{2}}{R_{elem}}=\frac{6.0^{2}}{0.85}\approx42.4\,\mathrm{W}
\label{eq:pwire}
\end{equation}

Using PWM to limit the effective voltage below the ignition threshold until arm state is confirmed prevents unintended heating on the bench. Bench characterisation with 26~AWG elements showed that at minimum PWM duty cycle the wire withstood approximately 30~seconds of sustained energisation without failure, compared to burnout within 2~seconds at maximum (DC-equivalent) duty. Pyrotechnic ignition and pop tests have not yet been conducted and are scheduled for the next project phase. The 8-bit timer command that produces the target voltage is calculated in (b) below.

\textbf{(b) PWM duty-cycle model:}

The duty-cycle model used for target igniter voltage is:
\begin{equation}
D = \frac{V_{target}}{V_{supply}}
\label{eq:pwm_duty}
\end{equation}

For the nominal case with $V_{target}=6.0$~V and $V_{supply}=15.0$~V:
\begin{equation}
D = \frac{6.0}{15.0}=0.40
\label{eq:pwm_duty_value}
\end{equation}

With 8-bit PWM resolution, the corresponding timer command value is:
\begin{equation}
\mathrm{PWM}_{cmd}=D(2^8-1)=0.40\times255\approx102
\label{eq:pwm_cmd}
\end{equation}

\textbf{(c) IRLZ44N vs IRF-type MOSFET choice:}

The selected switch is IRLZ44N (logic-level) rather than an IRF non-logic-level alternative, because ESP32 gate drive is 3.3~V and logic-level devices reach much lower effective \(R_{DS(on)}\) under low gate voltage operation \cite{irlz44n_datasheet, mosfet_switching}. The IRLZ44N is rated $V_{DSS}=55$~V and $I_D=47$~A with a datasheet $R_{DS(on)}=22$~m$\Omega$ at $V_{GS}=5$~V---fully compatible with 3.3~V logic gate drive, whereas standard IRF-series parts require $V_{GS}\geq10$~V to reach low-resistance operation \cite{irlz44n_datasheet}.

Conduction loss scales as:
\begin{equation}
P_{cond}=I^2R_{DS(on)}
\label{eq:pcond}
\end{equation}

For a representative $I=3$~A pulse, a low-\(R_{DS(on)}\) logic-level case (e.g., \(R\approx0.022\,\Omega\)) gives:
\begin{equation}
P_{cond,IRLZ}\approx3^2\times0.022\approx0.20\,\mathrm{W}
\label{eq:pcond_irlz}
\end{equation}

Whereas a higher-resistance non-logic-level case at low gate drive (e.g., \(R\approx0.05\,\Omega\) or more) gives:
\begin{equation}
P_{cond,IRF}\gtrsim3^2\times0.05\gtrsim0.45\,\mathrm{W}
\label{eq:pcond_irf}
\end{equation}

This lower dissipation and stronger 3.3~V gate compatibility motivated selecting IRLZ44N for the deployment channels.

\subsubsection{Additional Implementation Details}

To complete the electrical-deployment pairing, the design also includes:
\begin{itemize}
    \item Hardware serial pin remapping for XBee RX/TX on ESP32 without blocking I$^2$C monitoring.
    \item Segregated low-voltage logic and high-energy pyro routing on PCB for reduced coupling.
    \item Pre-arm validation of battery/chute channels before enabling deployment PWM outputs.
\end{itemize}

\subsection{Structural (Mechanical) Module Design Methodology}

\subsubsection{Structural Module Scope}

The structural module defines the flight-computer holder, battery-carrier geometry, threaded-rod integration, mounting-hole allocation, and interface provision for the XBee extension board and battery management system (BMS). Beyond individual component retention, the holder functions as the primary mechanical bed that fastens the complete battery pack as a unified assembly and provides the upper-platform mounting surface for the XBee extension board and beacon antenna module.

\subsubsection{Structural Design Considerations}

The structural design constraints are:
\begin{itemize}
    \item Fit within the 95~mm avionics-bay envelope.
    \item Survive launch and deployment shock/vibration events (design baseline: up to 4g sustained/inertial loading, with higher short-duration pop-test transients).
    \item Package four 18650 cells in parallel while avoiding rod interference.
    \item Maintain two threaded rods at 50~mm spacing and approximately 6~mm offset from the rear holder reference edge.
    \item Raise edge battery seats relative to mid seats so outer cells clear rod paths.
    \item Allocate and distribute self-tapping screw locations for load sharing.
    \item Provide mounting provision above battery holders for XBee extension and BMS.
    \item Include threaded holes for brass columns used to mount the flight computer.
\end{itemize}

\subsubsection{Structural Parts List}

\begin{table}[htbp]
\centering
\caption{Structural Module Parts List}
\label{tab:structural_parts}
\small
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{@{}p{3.4cm}|p{3.1cm}|p{6.7cm}@{}}
\toprule
\rowcolor{gray!22}\textbf{Part} & \textbf{Nominal Quantity} & \textbf{Function in Structural Module} \\ \midrule
Flight-computer holder body (PETG) & 1 & Primary load-carrying printed structure supporting electronics and battery fixtures \\
18650 cells & 4 & Parallel energy source mounted in dedicated mid/edge battery carriers \\
Mid battery holders (shorter) & 2 & Support central cells while preserving assembly height and alignment envelope \\
Edge battery holders (raised) & 2 & Raise outer cells to clear threaded-rod trajectories \\
Threaded rods & 2 & Structural retention members at 50~mm spacing and 6~mm rear offset reference \\
Self-tapping screws & multiple & Distributed fastening points for force sharing and anti-vibration retention \\
Brass columns (standoffs) & 4 & Flight-computer mounting through threaded holes in the holder \\
XBee extension mount + BMS mount area & 1 each & Allocated upper-plane features for electronics integration above cell carriers \\ \bottomrule
\end{tabular}
\end{table}

\subsubsection{Structural Design Option Comparison}

\begin{table}[htbp]
\centering
\caption{Structural Design Comparison Against Mechanical Requirements}
\label{tab:structural_option_compare}
\small
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{@{}p{2.8cm}|p{2.1cm}|p{2.5cm}|p{2.5cm}|p{3.5cm}@{}}
\toprule
\rowcolor{gray!22}\textbf{Option} & \textbf{Envelope Fit (95~mm)} & \textbf{Shock/Vibration Robustness} & \textbf{Assembly/Integration} & \textbf{Assessment} \\ \midrule
Single-level battery plane & Good & Medium--low & Simple & Rod interference risk for edge cells; weaker shock retention \\
Adhesive-only retention & Good & Low & Simple initially & Poor serviceability and limited repeatability under vibration/pop tests \\
\textbf{Stepped holders + rods + screws} & \textbf{Good} & \textbf{High} & \textbf{Moderate} & \textbf{Best balance of fit, force distribution, maintainability, and subsystem integration} \\ \bottomrule
\end{tabular}
\end{table}

\subsubsection{Chosen Structural Design and Implementation}

The selected design is a stepped battery-holder architecture with raised edge supports, shorter mid supports, dual threaded-rod retention, and distributed self-tapping screw points. This combination provides geometric clearance and better load sharing under launch and pop-test disturbances. The holder additionally fastens the complete 4-cell battery pack as a single retained unit, and the upper platform above the battery carriers serves as the mounting bed for both the XBee extension board and the beacon antenna module, keeping all avionics electronics within the 95~mm bay envelope.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.62\textwidth]{{images/mechanical/FC_Holder_without title block.JPG}}
\caption{Flight-computer holder sled: stepped battery seating, load path, and upper electronics mounting platform}
\label{fig:cad_fc_holder_no_block}
\par\smallskip
{\small The sled fastens the complete 4-cell pack as a unified assembly and serves as the mounting bed for the XBee extension board and beacon antenna above the battery carriers.}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.62\textwidth]{{images/mechanical/18650_case_without title block.JPG}}
\caption{18650 battery case for the stepped carrier assembly}
\label{fig:cad_18650_case_no_block}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.62\textwidth]{{images/mechanical/Edge_batt_without title block.JPG}}
\caption{Raised edge battery support clearing the threaded-rod path}
\label{fig:cad_edge_batt_no_block}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.62\textwidth]{{images/mechanical/Mid_batt_without title block.JPG}}
\caption{Mid battery support for controlled pack height and fit}
\label{fig:cad_mid_batt_no_block}
\end{figure}

\subsubsection{Structural Load Calculations}

\paragraph{(a) 18650 battery mass and weight}
Using a representative 18650 cell mass of $m_{cell}\approx0.047$~kg (typical of high-capacity 18650 datasheets), total cell mass is \cite{panasonic_cgr18650da_datasheet}:
\begin{equation}
m_{pack}=4m_{cell}=4(0.047)=0.188\,\mathrm{kg}
\label{eq:mpack}
\end{equation}

Static gravitational load from the four cells:
\begin{equation}
W_{pack}=m_{pack}g=0.188\times9.81\approx1.84\,\mathrm{N}
\label{eq:wpack}
\end{equation}

\paragraph{(b) Launch inertial force (4g design case)}
For a 4g launch-load envelope,
\begin{equation}
a_{launch}=4g=39.24\,\mathrm{m/s^2}
\label{eq:alaunch}
\end{equation}

Equivalent inertial load from the 4-cell pack:
\begin{equation}
F_{launch}=m_{pack}a_{launch}=0.188\times39.24\approx7.38\,\mathrm{N}
\label{eq:flaunch}
\end{equation}

This follows Newtonian net-force treatment used in amateur rocket performance analysis (thrust-driven acceleration and structural loading) \cite{nakka_thrust, nakka_eqns}.

\paragraph{(c) Pop-test pressure-driven force (188~kPa reference)}
Using the previously observed pop-test pressure reference $P_{pop}=188$~kPa and a conservative holder projected area approximated by the 95~mm bay section:
\begin{equation}
A_{95}=\pi\left(\frac{0.095}{2}\right)^2\approx7.09\times10^{-3}\,\mathrm{m^2}
\label{eq:a95}
\end{equation}

Pressure-driven resultant force estimate:
\begin{equation}
F_{pop}=P_{pop}A_{95}=188000\times7.09\times10^{-3}\approx1.33\times10^3\,\mathrm{N}
\label{eq:fpop}
\end{equation}

\paragraph{(d) Impulse estimate for pop transient}
For a representative short pressure-pulse duration $\Delta t$,
\begin{equation}
J=F_{pop}\Delta t
\label{eq:impulse_pop}
\end{equation}

At $\Delta t=10$~ms, the impulse estimate is:
\begin{equation}
J\approx1330\times0.01\approx13.3\,\mathrm{N\cdot s}
\label{eq:impulse_pop_value}
\end{equation}

\paragraph{(e) Load distribution through fasteners and supports}
If the primary retaining load is shared by $n$ self-tapping screw points, first-order load per point is:
\begin{equation}
F_{point}=\frac{F}{n}
\label{eq:fpoint}
\end{equation}

For example, under $F_{pop}\approx1330$~N: 4 points would carry $\sim332$~N each, while 8 points would carry $\sim166$~N each. This demonstrates why distributed fastening and multiple support interfaces reduce local stress concentration and improve structural reliability.

\subsection{Embedded Software Methodology}

\subsubsection{Software Module Scope}

The software module governs all firmware executing on the ESP32 flight computer, covering three implementation areas: (i)~telemetry and XBee communication management, (ii)~pyrotechnic deployment state-machine control via PWM, and (iii)~power and continuity monitoring integration. Area~(iii) is in active development and has not yet been validated against live hardware at the time of this report.

\subsubsection{Software Design Considerations}

Three core problems drive the software design:

\begin{enumerate}
    \item \textbf{XBee Integration:} The XBee-PRO 900HP must stream telemetry reliably at a deterministic data rate using hardware serial. Interface selection (SPI vs UART), data rate constraints, and pre-flight radio configuration via XCTU must all be resolved before firmware integration.
    \item \textbf{Pyrotechnic Deployment via PWM:} The deployment state machine must safely arm, fire, and confirm chute deployment events using PWM-gated MOSFET drive without false actuation or premature triggering under vibration or electromagnetic interference.
    \item \textbf{Power Monitoring Integration:} Real-time battery voltage and chute-channel continuity feedback from the ADS1115 must be incorporated into pre-flight arm logic and appended to the outbound telemetry packet. This integration is not yet complete in the current firmware build.
\end{enumerate}

\subsubsection{XBee Communication Interface: SPI vs UART}

The XBee-PRO 900HP hardware exposes only a UART serial interface for transparent-mode data transfer; it does not offer SPI or I\textsuperscript{2}C bus modes \cite{xbee_manual}. SPI is therefore inapplicable, and the design uses the ESP32 hardware UART peripheral with remapped RX/TX GPIO pins, preserving all other peripheral assignments on the flight computer \cite{esp32_datasheet}.

Key serial parameters governing the XBee-to-ESP32 link are listed in Table~\ref{tab:xbee_serial_params}.

\begin{table}[htbp]
\centering
\caption{XBee-PRO 900HP Serial Interface Parameters}
\label{tab:xbee_serial_params}
\small
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{@{}p{3.8cm}|p{3.0cm}|p{5.9cm}@{}}
\toprule
\rowcolor{gray!22}\textbf{Parameter} & \textbf{Value} & \textbf{Implementation Note} \\ \midrule
Default baud rate & 9600 baud & Factory default; must be updated via XCTU \texttt{BD} command \\
N4 firmware baud rate & 115200 baud & Reduces per-packet latency; set identically on flight and ground units \\
Over-air RF data rate & 200 kbps & Fixed by module hardware; not configurable from firmware side \\
Interface type & UART, 3.3~V logic & No SPI path; transparent mode passes raw bytes directly to/from ESP32 \\
Telemetry packet interval & 100~ms (10~Hz) & Frame rate used in N4 build; well below over-air throughput ceiling \\ \bottomrule
\end{tabular}
\end{table}

\subsubsection{Radio Pre-Flight Configuration via XCTU}

XCTU is Digi's official configuration and test utility for XBee modules. The N4 pre-flight procedure programs both flight and ground-station units via XCTU before each integration cycle \cite{xbee_manual}:

\begin{enumerate}
    \item \textbf{Network ID (ID):} Set identically on both units (e.g., \texttt{7FFF}) to isolate the N4 network from other XBee traffic in the field.
    \item \textbf{Baud Rate (BD):} Set to 115200 on both units to match firmware UART initialization.
    \item \textbf{Channel Mask:} 900~MHz ISM band frequency-hopping profile configured to minimize ambient interference.
    \item \textbf{Transmit Power (PL):} Maximum (+24~dBm) for full downlink link-margin.
    \item \textbf{API Mode (AP):} Set to 0 (transparent mode) so the module passes raw bytes directly without packet-framing overhead, which suits the simple CSV/binary telemetry stream.
    \item \textbf{Sleep Mode (SM):} Disabled; module remains active continuously during all flight phases.
\end{enumerate}

After configuration, a loopback range test confirms bidirectional packet delivery before avionics bay assembly. Module profiles are saved in XCTU for rapid re-flashing if hardware replacement is required before launch.

\subsubsection{Firmware State Machine Architecture}

The firmware is structured as a finite-state machine (FSM) executed inside the ESP32 main \texttt{loop()}, with telemetry transmission running continuously across all states via hardware serial interrupt.

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
  >=Latex,
  state/.style={draw, rectangle, rounded corners=6pt, fill=gray!12, minimum width=2.0cm,
                minimum height=0.9cm, align=center, font=\small\bfseries, text width=2.0cm},
  lbl/.style={font=\scriptsize, align=center, text width=2.2cm},
  arr/.style={->, thick, shorten >=2pt, shorten <=2pt}
]
  \node[state] (idle)     {IDLE};
  \node[state, right=2.0cm of idle]   (armed)   {ARMED};
  \node[state, right=2.0cm of armed]  (flight)  {FLIGHT};
  \node[state, below=1.8cm of flight] (apogee)  {APOGEE};
  \node[state, left=5.8cm of apogee]  (recovery){RECOVERY};

  \draw[arr] (idle)     -- node[above, font=\scriptsize, align=center]{arm cmd\\bat~$>$14~V} (armed);
  \draw[arr] (armed)    -- node[above, font=\scriptsize, align=center]{launch\\event} (flight);
  \draw[arr] (flight)   -- node[right, font=\scriptsize, align=center]{apogee\\detected} (apogee);
  \draw[arr] (apogee)   -- node[above, font=\scriptsize, align=center]{altitude\\threshold} (recovery);
  \draw[arr] (recovery.west) -- ++(-0.6,0) |- node[left, pos=0.25, font=\scriptsize]{landed/timeout} (idle.west);

  \node[lbl, below=0.25cm of idle]    {\textit{PWM off}\\pre-arm checks};
  \node[lbl, below=0.25cm of armed]   {\textit{continuity ok}\\PWM enabled};
  \node[lbl, below=0.25cm of flight]  {10~Hz telemetry\\IMU\,+\,baro};
  \node[lbl, below=0.25cm of apogee]  {drogue fire\\$D=0.40$};
  \node[lbl, below=0.25cm of recovery]{main fire\\RSSI track};
\end{tikzpicture}
\caption{N4 flight firmware state machine: operational states, transitions, and per-state behaviour}
\label{fig:sw_state_machine}
\end{figure}

\subsubsection{Pyrotechnic Deployment Software Flow}

The deployment channel is driven only when the FSM reaches the APOGEE or RECOVERY state and all pre-fire checks pass. Figure~\ref{fig:sw_deploy_flow} shows the gating logic.

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
  >=Latex,
  proc/.style={draw, rectangle, rounded corners=4pt, fill=gray!10, minimum width=6.2cm,
               minimum height=0.72cm, align=center, font=\small, text width=6.0cm},
  dec/.style={draw, diamond, fill=yellow!18, aspect=2.6, align=center,
              font=\small, inner sep=1pt, text width=3.2cm},
  term/.style={draw, rectangle, rounded corners=10pt, fill=green!14, minimum width=6.2cm,
               minimum height=0.72cm, align=center, font=\small, text width=6.0cm},
  side/.style={draw, rectangle, rounded corners=3pt, fill=red!12, minimum width=3.8cm,
               minimum height=0.68cm, align=center, font=\scriptsize, text width=3.6cm},
  arr/.style={->, thick, shorten >=2pt, shorten <=2pt}
]
  \node[term] (start)  {\texttt{loop()} --- read FSM state, baro, ADC};
  \node[dec,  below=0.45cm of start]  (fsm)    {State = APOGEE\\or RECOVERY?};
  \node[proc, below=0.55cm of fsm]    (check)  {Check: bat\,$>$\,14.0~V AND continuity\,$>$\,1000 counts};
  \node[dec,  below=0.45cm of check]  (armq)   {Arm conditions\\met?};
  \node[proc, below=0.55cm of armq]   (fire)   {\texttt{ledcWrite(CH,~102)}\quad$D=0.40$\quad wait ignition window};
  \node[proc, below=0.45cm of fire]   (poll)   {Poll ADC; count drops below threshold\,$\to$\,deploy confirmed};
  \node[term, below=0.45cm of poll]   (done)   {\texttt{ledcWrite(CH,~0)}; log flag; XBee TX confirmation};

  \node[side, right=1.2cm of fsm]   (hold)  {Hold; TX status\\packet; return};
  \node[side, right=1.2cm of armq]  (inhib) {Set \texttt{PWR\_INHIBIT};\\TX warning; block};

  \draw[arr] (start) -- (fsm);
  \draw[arr] (fsm)   -- node[left, font=\scriptsize]{Yes} (check);
  \draw[arr] (fsm)  -| node[above, pos=0.25, font=\scriptsize]{No}  (hold);
  \draw[arr] (check) -- (armq);
  \draw[arr] (armq)  -- node[left, font=\scriptsize]{Yes} (fire);
  \draw[arr] (armq) -| node[above, pos=0.25, font=\scriptsize]{No}  (inhib);
  \draw[arr] (fire)  -- (poll);
  \draw[arr] (poll)  -- (done);
\end{tikzpicture}
\caption{Deployment channel firmware flow: arm checks, PWM fire command, and post-event confirmation}
\label{fig:sw_deploy_flow}
\end{figure}

Key firmware constants for the deployment channel \cite{n4_pyro_control, n4_pwm_config}:
\begin{itemize}
    \item \texttt{PWM\_DUTY\_IGNITE = 102} \quad ($D = 0.40$, $V_{\mathrm{eff}} \approx 6$~V from 15~V supply)
    \item \texttt{LAUNCH\_INHIBIT\_V = 14.0}~V \quad minimum battery voltage for arm enable
    \item \texttt{CHUTE\_COUNT\_THRESHOLD = 1000} \quad ADS1115 ADC counts for continuity present
    \item \texttt{PWM\_FREQ = 1000}~Hz \quad LEDC timer frequency for the ignition channel
\end{itemize}

\subsubsection{Power Monitoring Software Integration (In Progress)}

Battery voltage and chute-channel continuity are read from the ADS1115 over I\textsuperscript{2}C and back-calculated to real-world voltage using the inverse of the hardware voltage divider \cite{ads1115_datasheet}:
\begin{equation}
V_{battery} = V_{ADC} \times \frac{57.0}{10.0}
\label{eq:vbatt_sw}
\end{equation}

The reconstructed voltage is appended to the telemetry packet and compared against the inhibit threshold. If the value falls below \texttt{LAUNCH\_INHIBIT\_V}, the deployment arm flag is cleared and a ``\texttt{PWR INHIBIT}'' status byte is broadcast over XBee to the ground station before launch \cite{n4_pyro_control}.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.78\textwidth, trim=0 12mm 0 0, clip]{images/Power_Monitoring_Design_Concept.png}
\caption{Power monitoring concept: ADS1115 I\textsuperscript{2}C polling, voltage reconstruction, and launch-inhibit logic}
\label{fig:sw_power_concept}
\end{figure}

\textbf{Current integration status:} The hardware design and Proteus simulation of the ADS1115 monitoring circuit are complete (detailed in Section~\ref{sec:methodology}, Electrical Module). Firmware I\textsuperscript{2}C polling of ADS1115 channels and the voltage reconstruction function are coded but have not yet been validated against the assembled PCB. Integration testing is planned as the next milestone before launch readiness review.

\subsection{Validation and Test Method}

Testing is staged from component to full-system level:
\begin{itemize}
    \item \textbf{Component level:} PWM-output verification, continuity checks, and ADC calibration.
    \item \textbf{Subsystem level:} telemetry range trials, ignition repeatability tests, and enclosure leak/charring checks.
    \item \textbf{System level:} integrated ground rehearsal with full communication stack and deployment command-path validation.
\end{itemize}


